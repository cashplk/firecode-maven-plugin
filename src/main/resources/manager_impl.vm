package ${base}#if("${module}"!="").${module}#end.manager.impl;

import ${base}#if("${module}"!="").${module}#end.model.param.Create${clazzName}Param;
import ${base}#if("${module}"!="").${module}#end.model.param.Update${clazzName}Param;
import ${base}#if("${module}"!="").${module}#end.model.query.${clazzName}Query;
import ${base}#if("${module}"!="").${module}#end.model.dos.${clazzName}DO;
import ${base}#if("${module}"!="").${module}#end.model.query.${clazzName}Query;
import ${base}#if("${module}"!="").${module}#end.mapper.${clazzName}Mapper;
import ${base}#if("${module}"!="").${module}#end.manager.${clazzName}Manager;

import org.springframework.stereotype.Component;
import java.util.List;
import javax.annotation.Resource;


/**
* Created by ${author}
* Time ${time}
* Desc
*/
@Component
public class ${clazzName}ManagerImpl implements ${clazzName}Manager{

@Resource
private ${clazzName}Mapper ${javaName}Mapper;

@Override
public ${clazzName}DO get${clazzName}(${clazzName}Query ${javaName}Query) {
return ${javaName}Mapper.get${clazzName}(${javaName}Query);
}

@Override
public Long create${clazzName}(${clazzName}DO ${javaName}DO) {
${javaName}Mapper.insert${clazzName}(${javaName}DO);
return ${javaName}DO.getId();
}

@Override
public int update${clazzName}(Update${clazzName}Param update${clazzName}Param) {
return ${javaName}Mapper.update${clazzName}(update${clazzName}Param);
}

@Override
public int count${clazzName}(${clazzName}Query ${javaName}Query) {
return ${javaName}Mapper.count${clazzName}(${javaName}Query);
}

@Override
public List
<${clazzName}DO> list${clazzName}(${clazzName}Query ${javaName}Query) {
    return  ${javaName}Mapper.list${clazzName}(${javaName}Query);
    }
    }