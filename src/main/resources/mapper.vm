package ${base}#if("${module}"!="").${module}#end.mapper;


import ${base}#if("${module}"!="").${module}#end.model.dos.${clazzName}DO;
import ${base}#if("${module}"!="").${module}#end.model.param.Update${clazzName}Param;

import ${base}#if("${module}"!="").${module}#end.model.query.${clazzName}Query;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

import java.util.List;


/**
* Created by ${author}
* Time ${time}
* Desc
*/
@Mapper
public interface ${clazzName}Mapper {

/**
* 查询
* @param ${javaName}Query
* @return
*/
${clazzName}DO get${clazzName}(${clazzName}Query ${javaName}Query);

/**
* COUNT
* @param ${javaName}Query
* @return
*/
int count${clazzName}(${clazzName}Query ${javaName}Query);

/**
* 查询列表
* @param ${javaName}Query
* @return
*/
List
<${clazzName}DO> list${clazzName}(${clazzName}Query ${javaName}Query);

    /**
    * 新增
    * @param ${javaName}DO
    * @return
    */
    int insert${clazzName}(${clazzName}DO ${javaName}DO);

    /**
    * 更新
    * @param update${clazzName}Param
    * @return
    */
    int update${clazzName}(Update${clazzName}Param update${clazzName}Param);
    }